# ============================================
# BookLeaf Cover Validator — Dependencies
# Install: pip install -r requirements.txt
# ============================================

# --- Core web framework ---
flask==3.1.3
werkzeug==3.1.8          # secure_filename, file handling — was missing, Flask depends on it

# --- AI vision ---
google-genai==2.6.0
ultralytics==8.2.0
easyocr==1.7.1
opencv-python-headless==4.9.0.80

# --- Image processing ---
Pillow==12.2.0           # PIL.Image — loads PNG/JPG for Gemini

# --- HTTP & integrations ---
requests==2.34.2         # Airtable REST API calls

# --- Config ---
python-dotenv==1.2.2     # loads .env for API keys

# --- Google Drive polling (optional) ---
# Required only if running: python pipeline.py --drive
# Comment out if you don't need Drive integration
google-api-python-client==2.196.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0